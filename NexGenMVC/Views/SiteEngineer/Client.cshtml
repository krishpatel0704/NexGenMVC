@model IEnumerable<NexGenMVC.Models.Fn_GetClint_Result>

@{
    ViewBag.Title = "Client";
    Layout = "~/Views/Shared/_SiteEngineerLayoutPage.cshtml";
}

<h2>Client</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    <a href="@Url.Action("NewClient", "SiteEngineer", "NewClient")" class="elements"><span>New Client</span></a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.clientName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.clientDistrict)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.clientDesLocation)
        </th>
        <th></th>
    </tr>

@foreach (var item in ViewData.Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.clientName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.clientDistrict)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.clientDesLocation)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.clientId }) |
            @Html.ActionLink("Details", "Details", new { id=item.clientId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.clientId })
        </td>*@
    </tr>
}

</table>
